# إعداد تسجيل الدخول

## مفتاح توقيع الدخول

يجب ضبط `SESSION_SECRET` في ملف `.env` محليًا وفي إعدادات الاستضافة عند النشر. التطبيق يرفض التشغيل إذا كان المفتاح مفقودًا أو فارغًا أو يساوي أحد المفاتيح الافتراضية القديمة.

لإنشاء مفتاح جديد، استخدم `openssl rand -hex 32` واحفظ الناتج في إعدادات البيئة بشكل سري. تغيير المفتاح يبطل صلاحية رموز الدخول القديمة، ويحتاج المستخدمون لتسجيل الدخول مجددًا. لا حاجة لتغيير المفتاح الحالي لمجرد تطبيق هذا التحديث إذا كان مخصصًا وسريًا.

## إنشاء أول أدمن

صفحة تسجيل الدخول لا تنشئ حسابات. لإنشاء أول أدمن، اضبط `DATABASE_URL` و`DIRECT_DATABASE_URL` لقاعدة البيانات المقصودة، ثم شغّل الأمر التالي في Bash لتلقي كلمة المرور دون عرضها أو كتابتها في سجل الأوامر:

```bash
read -r -p 'Admin username: ' ADMIN_USERNAME
read -r -s -p 'Admin password: ' ADMIN_PASSWORD
printf '\n'
export ADMIN_USERNAME ADMIN_PASSWORD
npm run setup:admin
unset ADMIN_USERNAME ADMIN_PASSWORD
```

كلمة المرور لا تقل عن 12 حرفًا ولا تزيد عن 72 بايت بترميز UTF-8. لا تحفظ `ADMIN_PASSWORD` في المستودع أو إعدادات النشر الدائمة.

الأمر يرفض التنفيذ إذا وجد أدمن بالفعل أو كان اسم المستخدم مستخدمًا. ولا يعيد تعيين كلمات المرور أو يغير صلاحيات الحسابات الموجودة. إنشاء الحساب محمي بقفل داخل معاملة لمنع إنشاء أكثر من أدمن عند تشغيل الإعداد بالتزامن.

سكريبت `npm run seed` يحافظ الآن على حسابات المستخدمين ولا ينشئ كلمة مرور افتراضية. لكنه ما زال يحذف بيانات العملاء والفواتير لإعادة استيرادها؛ ليس أمر إعداد حسابات.

## التحقق

شغّل `npm test`. اختبارات تسجيل الدخول والإعداد تستخدم قاعدة بيانات وهمية، ولا تنشئ أو تعدّل حسابات حقيقية.
